# Use the UTF-8 locale provisioned by the Linux setup scripts and available by
# default on macOS. Fall back cleanly while bootstrapping an existing machine;
# exporting a locale that does not exist makes tools such as manpath print
# during shell startup and trips Powerlevel10k's instant-prompt warning.
if LC_ALL=en_US.UTF-8 locale charmap 2>/dev/null | grep -qx UTF-8; then
    export LANG=en_US.UTF-8
elif LC_ALL=C.UTF-8 locale charmap 2>/dev/null | grep -qx UTF-8; then
    export LANG=C.UTF-8
else
    export LANG=C
fi

# LANG provides defaults for every category; clearing overrides prevents SSH
# and Mosh from forwarding a stale, unavailable locale.
unset LC_ALL LC_CTYPE

# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block, everything else may go below.
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

# Keep PATH-like arrays free of duplicates. Any subsequent `export PATH=...`
# that prepends/appends an already-present entry is silently deduped.
typeset -U path PATH fpath FPATH

# macOS uses Homebrew Neovim; Linux keeps the /opt/nvim tarball ahead of
# distro packages.
if [[ $OSTYPE == darwin* ]]; then
    path=(${path:#/opt/nvim/bin})
elif [[ $OSTYPE == linux* ]]; then
    path=(/opt/nvim/bin $path)
fi

if command -v nvim >/dev/null 2>&1; then
    export EDITOR="$(which nvim)"
    export SUDO_EDITOR="$(which nvim)"
elif command -v vim >/dev/null 2>&1; then
    export EDITOR="$(which vim)"
    export SUDO_EDITOR="$(which vim)"
else
    export EDITOR=vi
    export SUDO_EDITOR=vi
fi

export ZSH="$HOME/.oh-my-zsh"
ZSH_THEME="powerlevel10k/powerlevel10k"

# -----------------------------------------------------
# oh-myzsh plugins
# -----------------------------------------------------
plugins=(
    git
    sudo
    web-search
    archlinux
    zsh-autosuggestions
    zsh-syntax-highlighting
    fast-syntax-highlighting
    copyfile
    copybuffer
    dirhistory
)

if command -v brew >/dev/null 2>&1; then
    plugins+=(brew)
fi

if command -v node >/dev/null 2>&1; then
    plugins+=(node)
fi

if command -v nvm >/dev/null 2>&1; then
    plugins+=(nvm)
fi

if command -v docker >/dev/null 2>&1; then
    plugins+=(docker docker-compose)
fi

# Set-up oh-my-zsh
source $ZSH/oh-my-zsh.sh

# -----------------------------------------------------
# Set-up FZF key bindings (CTRL R for fuzzy history finder)
# -----------------------------------------------------
if command -v fzf >/dev/null 2>&1; then
    if fzf_init="$(fzf --zsh 2>/dev/null)"; then
        eval "$fzf_init"
    elif [[ -f /usr/share/doc/fzf/examples/key-bindings.zsh ]]; then
        source /usr/share/doc/fzf/examples/key-bindings.zsh
    fi
fi

# zsh history
HISTFILE=~/.zsh_history
HISTSIZE=10000
SAVEHIST=10000
setopt appendhistory

# Aliases
alias ll="ls -lah"
if command -v eza >/dev/null 2>&1; then
    alias ls='eza -a --icons=always'
    alias ll='eza -al --icons=always'
    alias lt='eza -a --tree --level=1 --icons=always'
    alias lt2='eza -a --tree --level=2 --icons=always'
fi
alias yt-dlp-best="yt-dlp --external-downloader=aria2c --external-downloader-args '--min-split-size=1M --max-connection-per-server=16 --max-concurrent-downloads=16 --split=16' -f '(bestvideo+bestaudio/best)' --merge-output-format 'mkv'"
alias drma='docker rm -f $(docker ps -a -q)'

alias ff='fastfetch'

# take ownership and set permissions to 775 for dirs and 664 for files 
alias fixperms='sudo chown -R $(whoami):$(whoami) . && sudo chmod -R a=,a+rX,u+w,g+w .'

alias lg='lazygit'
alias c='claude --dangerously-skip-permissions'
alias cdx='codex --dangerously-bypass-approvals-and-sandbox'
alias hl='rg --passthrough'

# Clear bunx's per-package temp caches under $TMPDIR. Useful when a cached
# install is corrupt (e.g. partial node_modules from an interrupted download
# causing ERR_MODULE_NOT_FOUND). The (N) qualifier is zsh's NULL_GLOB so
# this is a no-op when nothing matches instead of erroring.
bunx-clean() {
  local tmp="${TMPDIR:-/tmp}"
  rm -rf "${tmp%/}"/bunx-*(N)
}

# ssh with downgraded TERM for hosts that don't have terminal-specific
# terminfo (xterm-ghostty, xterm-kitty, alacritty, etc.). Fixes
# backspace/arrow-key issues. For your own machines, plain `ssh` works.
# To teach a remote host about your real TERM, run `push-terminfo HOST` once.
alias dssh='TERM=xterm-256color ssh'

# push-terminfo HOST: teach a remote host about your local $TERM by copying
# the terminfo entry. Fixes curses apps (bash readline, less, vim, tmux)
# that otherwise get garbage escape sequences for backspace, arrows, etc.
#
# Terminfo is a DB of terminal capabilities keyed by $TERM. Curses apps
# query it at startup. Search order: ~/.terminfo, /etc/terminfo,
# /usr/share/terminfo. If the remote has no entry for your terminal-specific
# $TERM, it falls back to generic defaults that don't match the emulator.
#
# How it works:
#   infocmp -a TERM        decompiles local compiled entry -> source form
#   tic -x -o DIR /dev/stdin  recompiles source back into a binary entry
#                             in DIR (~/.terminfo is user-level, no sudo)
#   -x                     accept extended caps (Ghostty/Kitty define many)
#
# tic and infocmp both ship with ncurses (ncurses-bin on Debian).
push-terminfo() {
  [[ -z "$1" ]] && { echo "usage: push-terminfo HOST"; return 1; }
  local entry term
  term="${TERM:-xterm-ghostty}"
  if ! entry=$(infocmp -a "$term" 2>/dev/null) || [[ -z "$entry" ]]; then
    echo "$term not in local terminfo DB" >&2
    return 1
  fi
  if print -r -- "$entry" | ssh "$1" "tic -x -o \$HOME/.terminfo /dev/stdin"; then
    echo "pushed $term to $1"
  else
    echo "remote tic error" >&2
    return 1
  fi
}

# dssh and push-terminfo both take an SSH host as first arg — reuse ssh's
# completion (which parses ~/.ssh/config + known_hosts).
compdef dssh=ssh 2>/dev/null
compdef push-terminfo=ssh 2>/dev/null

# Sync ~/.config/nvim with nvim-lua/kickstart.nvim. The fork's master branch
# remains a clean upstream mirror; custom receives ordinary merge commits so
# every machine can fast-forward without force-pushing rewritten history.
sync-nvim() {
  local sync_script="${DOTFILES_DIR:-$HOME/git/dotfiles}/lib/nvim-sync.sh"
  [[ -f "$sync_script" ]] || { echo "missing: $sync_script" >&2; return 1; }
  source "$sync_script"
  nvim_sync_fork "${XDG_CONFIG_HOME:-$HOME/.config}/nvim"
}

if command -v nvim >/dev/null 2>&1; then
    alias v="nvim"
    alias vi="nvim"
    alias vim="nvim"
    alias vimdiff="nvim -d"
elif command -v vim >/dev/null 2>&1; then
    alias v="vim"
    alias vi="vim"
    alias vimdiff="vim -d"
fi

function upd() {
  local failures=()

  _upd_run() {
    local label="$1"; shift
    local rc=0
    echo "\n\033[1;34m:: $label\033[0m"
    "$@"
    rc=$?
    if (( rc != 0 )); then
      printf '\033[1;31mcommand failed (%d): %s\033[0m\n' "$rc" "$(printf '%q ' "$@")"
      failures+=("$label")
    fi
    return $rc
  }

  _upd_run_bunx_skills() {
    local rc=0
    echo "\n\033[1;34m:: skills\033[0m"
    bunx skills update --global
    rc=$?

    if (( rc != 0 )); then
      echo "skills update failed; clearing bunx temp cache and retrying"
      bunx-clean
      bunx skills update --global
      rc=$?
    fi

    if (( rc != 0 )); then
      printf '\033[1;31mcommand failed (%d): %s\033[0m\n' "$rc" "bunx skills update --global"
      failures+=("skills")
    fi
    return $rc
  }

  # System packages
  if [[ $OSTYPE == linux* ]]; then
    if command -v paru >/dev/null 2>&1; then
      _upd_run "paru" paru -Syu --noconfirm
    elif command -v pacman >/dev/null 2>&1; then
      _upd_run "pacman" sudo pacman -Syu --noconfirm
    elif command -v apt >/dev/null 2>&1; then
      _upd_run "apt update" sudo apt update &&
        _upd_run "apt upgrade" sudo apt upgrade -y &&
        _upd_run "apt autoremove" sudo apt autoremove -y
    fi

    if command -v flatpak >/dev/null 2>&1; then
      _upd_run "flatpak" flatpak update -y
    fi
  fi

  if [[ $OSTYPE == darwin* ]]; then
    if command -v brew >/dev/null 2>&1; then
      _upd_run "brew update" brew update &&
        _upd_run "brew bundle" brew bundle --file="${DOTFILES_DIR:-$HOME/git/dotfiles}/Brewfile" &&
        _upd_run "brew upgrade" brew upgrade
    fi
  fi

  # Language toolchains
  if command -v rustup >/dev/null 2>&1; then
    _upd_run "rustup" rustup update
  fi

  # sdkman (sdk is a shell function, not a binary)
  if [[ -s "${SDKMAN_DIR:-$HOME/.sdkman}/bin/sdkman-init.sh" ]]; then
    # sdk upgrade prompts "Use prescribed default version(s)?" unless
    # sdkman_auto_answer=true; fix idempotently so upd stays non-interactive.
    local sdkman_cfg="${SDKMAN_DIR:-$HOME/.sdkman}/etc/config"
    if [[ -f "$sdkman_cfg" ]] && grep -q '^sdkman_auto_answer=false' "$sdkman_cfg"; then
      sed -i.bak 's/^sdkman_auto_answer=false/sdkman_auto_answer=true/' "$sdkman_cfg" && rm -f "${sdkman_cfg}.bak"
    fi
    _upd_run "sdkman self" sdk selfupdate
    _upd_run "sdkman candidates" sdk update
    _upd_run "sdkman upgrade" sdk upgrade
  fi

  if command -v bun >/dev/null 2>&1; then
    _upd_run "bun" bun upgrade
  fi

  if command -v npm >/dev/null 2>&1; then
    local npm_pkgs=($(npm outdated -g --parseable 2>/dev/null | cut -d: -f4 | sed 's/@[^@]*$//' | grep -v '/\.' | sort -u))
    if (( ${#npm_pkgs[@]} )); then
      _upd_run "npm global" npm update -g "${npm_pkgs[@]}"
    else
      echo "\n\033[1;34m:: npm global\033[0m"
      echo "all packages up to date"
    fi
  fi

  if command -v pnpm >/dev/null 2>&1; then
    # Corepack prompts before downloading a new pnpm tarball; suppress it.
    local pnpm_globals=""
    pnpm_globals=$(env COREPACK_ENABLE_DOWNLOAD_PROMPT=0 pnpm ls -g --depth=0 --parseable 2>/dev/null)
    if [[ -n "$pnpm_globals" ]]; then
      _upd_run "pnpm globals" env COREPACK_ENABLE_DOWNLOAD_PROMPT=0 pnpm update -g --latest
    else
      echo "\n\033[1;34m:: pnpm globals\033[0m"
      echo "no global packages installed"
    fi
  fi

  # Misc tools
  if command -v tldr >/dev/null 2>&1; then
    _upd_run "tldr" tldr -u
  fi

  # Upgrade Neovim before config/plugin synchronization so current Kickstart
  # and vim.pack APIs are available.
  local nvim_installer="${DOTFILES_DIR:-$HOME/git/dotfiles}/installers/install_neovim.sh"
  if [[ -f "$nvim_installer" ]]; then
    _upd_run "neovim" bash "$nvim_installer"
  fi

  # Keep petalas/nvim/master mirrored from Kickstart, merge upstream into the
  # custom branch, smoke-test it, and publish the merge. Ordinary merges let
  # other machines fast-forward safely without force-push reconciliation.
  local nvim_sync="${DOTFILES_DIR:-$HOME/git/dotfiles}/lib/nvim-sync.sh"
  local nvim_cfg="${XDG_CONFIG_HOME:-$HOME/.config}/nvim"
  if [[ -f "$nvim_sync" && -d "$nvim_cfg/.git" ]]; then
    source "$nvim_sync"
    if _upd_run "nvim config + upstream" nvim_sync_fork "$nvim_cfg"; then
      _upd_run "nvim plugins" nvim_update_plugins "$nvim_cfg"
    fi
  fi

  if command -v bunx >/dev/null 2>&1; then
    _upd_run_bunx_skills
  fi

  # Summary
  echo ""
  if (( ${#failures[@]} )); then
    echo "\033[1;31m✗ Failed: ${(j:, :)failures}\033[0m"
    unset -f _upd_run _upd_run_bunx_skills
    return 1
  else
    echo "\033[1;32m✓ Everything up to date.\033[0m"
  fi

  unset -f _upd_run _upd_run_bunx_skills
}

# kill anything taking up the specified port, usage example: killport 6060
function killport() {
  sudo lsof -i :$1 | grep LISTEN | awk '{ print $2 }' | xargs kill -9
}

# https://yazi-rs.github.io/docs/quick-start
# yazi wrapper that provides the ability to change the current working directory when exiting Yazi.
function y() {
    local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
    yazi "$@" --cwd-file="$tmp"
    if cwd="$(command cat -- "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
        builtin cd -- "$cwd"
    fi
    rm -f -- "$tmp"
}

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

# The next line updates PATH for the Google Cloud SDK.
if [ -f "$HOME/google-cloud-sdk/path.zsh.inc" ]; then . "$HOME/google-cloud-sdk/path.zsh.inc"; fi

# The next line enables shell command completion for gcloud.
if [ -f "$HOME/google-cloud-sdk/completion.zsh.inc" ]; then . "$HOME/google-cloud-sdk/completion.zsh.inc"; fi

# adding ~/.local/bin to path (for things like getnf)
export PATH="$HOME/.local/bin:$PATH"

# adding /opt/homebrew/sbin to path
export PATH="/opt/homebrew/sbin:$PATH"

# setup nvm
[[ -d "$HOME/.nvm" ]] && export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" # This loads nvm 
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion" # This loads nvm bash_completion
# setup nvm (homebrew version)
[ -s "/opt/homebrew/opt/nvm/nvm.sh" ] && . "/opt/homebrew/opt/nvm/nvm.sh"  # This loads nvm
[ -s "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm" ] && . "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm"  # This loads nvm bash_completion
[ -s "/usr/share/nvm/init-nvm.sh" ] && source "/usr/share/nvm/init-nvm.sh"

export PASSWORD_STORE_ENABLE_EXTENSIONS=true

# Add Rust's cargo bin to PATH if it's not already there
if [[ ":$PATH:" != *":$HOME/.cargo/bin:"* ]]; then
    export PATH="$HOME/.cargo/bin:$PATH"
fi

if command -v brew >/dev/null 2>&1; then
    export LIBRARY_PATH="$LIBRARY_PATH:$(brew --prefix)/lib"
    export PATH="/opt/homebrew/opt/ruby/bin:$PATH"
fi

# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$("$HOME/miniconda3/bin/conda" 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "$HOME/miniconda3/etc/profile.d/conda.sh" ]; then
        . "$HOME/miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="$HOME/miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<

# pnpm
export PNPM_HOME="$HOME/Library/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end
export PATH="$HOME/bin:$PATH"

# THIS MUST BE AT THE END OF THE FILE FOR SDKMAN TO WORK!!!
export SDKMAN_DIR="$HOME/.sdkman"
[[ -s "$HOME/.sdkman/bin/sdkman-init.sh" ]] && source "$HOME/.sdkman/bin/sdkman-init.sh"

# bun
if [[ -d "$HOME/.bun" ]]; then
  export BUN_INSTALL="$HOME/.bun"
  export PATH="$BUN_INSTALL/bin:$PATH"
  # bun completions
  [[ -s "$BUN_INSTALL/_bun" ]] && source "$BUN_INSTALL/_bun"
fi
# maestro
if [[ -d "$HOME/.maestro/bin" ]]; then
  export PATH="$PATH:$HOME/.maestro/bin"
fi

# android sdk (homebrew cmdline-tools)
if [[ -d "/opt/homebrew/share/android-commandlinetools" ]]; then
  export ANDROID_HOME="/opt/homebrew/share/android-commandlinetools"
  export PATH="$PATH:$ANDROID_HOME/emulator:$ANDROID_HOME/platform-tools"
fi

# lm studio cli (lms)
if [[ -d "$HOME/.lmstudio/bin" ]]; then
  export PATH="$PATH:$HOME/.lmstudio/bin"
fi

# Local machine-specific secrets and overrides. This file lives outside the
# dotfiles repo because ~/.zshrc is a symlink to dot/zshrc.
[[ -r "$HOME/.zshrc.local" ]] && source "$HOME/.zshrc.local"

# Vite+ bin (https://viteplus.dev)
[[ -r "$HOME/.vite-plus/env" ]] && source "$HOME/.vite-plus/env"
